{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="toast">
            <div class="body">
                {{ message }}
            </div>
        </div>
    {% endfor %}
{% endfor %}